> **⚠️ SUPERSEDED (task U3, 2026-07-25).** This fragment has been merged into
> `WSC/STATUS.md`, which is now the single authoritative table, and audited in
> `WSC/AUDIT.md`. It is kept as the per-task record only. Where it disagrees with
> `WSC/STATUS.md`, `WSC/STATUS.md` wins — the disagreements found by the U3 audit
> are itemised in `WSC/AUDIT.md` §7.

# status fragment — task V4 (composition layer + DirWF interval conjunct)

Baseline HEAD copied: `09bd428`. `lake build WSC` GREEN in the working copy
(382 jobs), all pre-existing shaped `blaster` verdicts unchanged
(`P4Shaped`/`P4ShapedIdx`/`P5Shaped`: `✅ Valid` ×N, `✅ Expected Falsified` ×N).

Deliverables: `WSC/Composition.lean` (NEW, ~1250 lines), `WSC/Honest.lean` (DirWF
conjunct (iv) + the bridge lemma), `WSC.lean` (imports the new module).

## Rows that change state

### `## 1. Property status table` — no P-row changes

No leaf property changed state. V4 added no `blaster` theorem and no new
faithfulness axiom.

### `### Composition` sub-table — REPLACE all three rows

| Item | State | Note |
|---|---|---|
| `WSC/Composition.lean` (invariant `I`, `Preservation`, chain induction, top claim) | **PROVED AS A REDUCTION** — the file exists and `lake build WSC` is green. `WSC.Composition.top_claim` and `no_programmable_tokens_outside_mini_ledger` are `sorry`-free Lean theorems, but they are stated over an explicit `leaves : LeafSet hp Shape` hypothesis bundle: the composition is unconditional, the LEAVES are not. **As of V4 none of the four `LeafSet` fields is fully discharged** — see §9.2 of that file. Never quote the top theorem as "the claim is proved"; quote it as "the claim reduces to exactly these four obligations". | The ledger vocabulary that was missing now exists: `UTxO`/`Ledger`/`OutOfBase`/`RegisteredIn`/`I`, and §5.3's `ts_genesis` + `lr_utxo_semantics` are stated there (plus `lr_inputs_in_ledger`, `lr_registration_source`, `LR_BALANCE_SLOT`, `ts_minting_identity_L`, `NONNEG_L`, `DIRWF_L`). |
| `L-monotone` (registration is insert-only) | **ASSUMED** — `DirWF` conjunct (i) per-tx; at ledger level it is `Composition.lr_registration_source` (UTxO semantics) plus `DIRWF_L`. | Discharged by U10. |
| `L-mint-needs-reg` | **STILL-OPEN, now stated** — `WSC.Composition.LeafSet.nopre`. | Depends on the FULL P4 (arms 1-3), the weakest leaf in the set. Nobody has started it. |

### `## 2. Axiom base` — ADD a row

| Group | Axioms | Discharged by |
|---|---|---|
| LEDGER-LEVEL (`WSC/Composition.lean`, task V4) | `LedgerStep`, `Genesis` (declared, not defined); `lr_utxo_semantics`, `lr_inputs_in_ledger`, `lr_registration_source`, `LR_BALANCE_SLOT`, `ts_genesis`, `ts_minting_identity_L`, `NONNEG_L`, `DIRWF_L` | Trusting the Cardano ledger (the `lr_*` four + `NONNEG_L`); deployment audit (`Genesis`/`ts_genesis`/`ts_minting_identity_L`); **U10** (`DIRWF_L`, ESCAPE-CRITICAL). `LR_BALANCE_SLOT` is the only one that is a mechanical consequence of an existing axiom (`LR7`/`isBalanced`) — the CLAB derivation is unwritten. |

Also amend the `DIRECTORY` row: `DIRWF` now has **FOUR** conjuncts (interval
non-overlap added by V4); same discharge (U10).

Also amend the `NOT STATED (deliberately)` row: §5.3 `ts_genesis` and
`lr_utxo_semantics` ARE now stated — in `WSC/Composition.lean`, as that row
predicted.

### `## 0. The three sentences that must never be dropped`

Sentence 3 can be sharpened: **`DirWF` is the single escape-critical assumption,
and since V4 the bridge it was missing is a theorem.**
`WSC.covering_node_excludes_registration` (per-tx, from `DIRWF`) and
`WSC.Composition.covering_excludes_ledger_registration` (ledger, from `DIRWF_L`)
are both proved, so ADDENDUM E3's required pair now exists and P5's
covering-node postcondition actually feeds the composition.

## New findings recorded in code (both were latent unsoundnesses, not typos)

1. **The interval-partition conjunct must be over the PRE-STATE snapshot.** Stated
   over `dirCandidates` (refs + inputs + OUTPUTS) it is FALSE for every directory
   INSERT: the insert produces a node keyed `cs` while the SPENT node it splits
   still shows `key < cs < next`. `WSC.dirPreState` (refs + inputs only) is the
   consistent snapshot. Docstring: `WSC/Honest.lean` `dirPreState`.
2. **`I(L)` is FALSE without `cs ≠ adaSymbol`.** ARCHITECTURE.md §5.1 writes the
   invariant with no exclusion; the directory HEAD SENTINEL is keyed with the empty
   `ByteString` (`PTokenDirectory.hs:216-223`) and `adaSymbol = ""`, so
   `RegisteredIn hp L adaSymbol` holds in every live deployment and the invariant
   would demand that no lovelace exists outside the mini-ledger. Docstring:
   `WSC/Composition.lean` `I`.

## Two repairs REPORTED, not made (they touch `WSC/Honest.lean` axioms)

1. **`LR_BUDGET_minting` / `MintingNonVacuous` name the wrong prep**
   (`appliedMinting.prop`, budget 600, machine-checked vacuous) while `K_mint = 900`
   and every P4 theorem lives on `appliedMinting900` / `appliedMintShaped900`. As
   written the minting budget bridge cannot be applied to anything. Same mismatch
   for `LR_BUDGET_global` / `GlobalNonVacuous` (`appliedGlobal.prop`, budget 600,
   probe-Valid vacuous) versus `appliedGlobal1600`. One-line repair each.
2. **`LR_BALANCE_SLOT` should be a CLAB theorem**, derived from `LR7`/`isBalanced`
   by distributing `valueOf` over `V2.merge` and splitting
   `valueSpent`/`valueProduced` along the payment-credential filter. Unwritten.

## New obligation created by the shaped-context route

**SHAPE BRIDGE** (`WSC/Composition.lean` §9.4):
`isSuccessful (appliedXShaped.prop args) ↔ isSuccessful (appliedX.prop (shapedCtx args))`.
Every shaped `by blaster` theorem speaks about a DIFFERENT prepped term than the
`LeafSet` fields do. Expected true, proved nowhere. Any claim that the composition
is discharged by shaped leaves must carry it.

## Measured axiom base of the top theorem

`#print axioms WSC.Composition.top_claim` (printed at build time, §9.7):
`propext, sorryAx, Classical.choice, Lean.ofReduceBool, Lean.trustCompiler,
Quot.sound, WSC.{Deployed, OnChain, LR_BUDGET_base, LR_CTX, LR_MINT_RUNS_POLICY,
LR_SPEND_RUNS_VALIDATOR, LR_WDRL_RUNS_VALIDATOR, NONNEG, NodeAcceptsBase,
NodeAcceptsGlobal, NodeAcceptsMinting, NodeAcceptsSeize, mlhPolicyId,
nodeStepsBase, nodeStepsGlobal, nodeStepsMinting},
WSC.Composition.{DIRWF_L, Genesis, LR_BALANCE_SLOT, LedgerStep, NONNEG_L,
lr_inputs_in_ledger, lr_registration_source, lr_utxo_semantics, ts_genesis,
ts_minting_identity_L}`.

`sorryAx` = `blaster`'s `admit` inside `P3_base_requires_global_or_seize`, reached
through `p3_lifted`. `Lean.ofReduceBool`/`Lean.trustCompiler` = `native_decide`,
reached through `baseNonVacuous`. NO `<model>_faithful` axiom appears — the
source-model route enters only when a `LeafSet` field is instantiated.
