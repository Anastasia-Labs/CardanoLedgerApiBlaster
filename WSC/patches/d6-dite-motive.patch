diff --git a/Blaster/Optimize/Basic.lean b/Blaster/Optimize/Basic.lean
index 29df8fe..b7da885 100644
--- a/Blaster/Optimize/Basic.lean
+++ b/Blaster/Optimize/Basic.lean
@@ -222,14 +222,30 @@ partial def optimizeExprAux (stack : List OptimizeStack) : TranslateEnvT Expr :=
     @[always_inline, inline]
     optimizeLambda
       (n : Name) (t : Expr) (b : Expr) (bi : BinderInfo)
-      (xs : List OptimizeStack) (inDite := false) : List OptimizeStack :=
-        .InitOptimizeExpr t :: .LambdaWaitForType n bi b inDite :: xs
-
+      (xs : List OptimizeStack) (diteBT : Option Expr := none) : List OptimizeStack :=
+        .InitOptimizeExpr t :: .LambdaWaitForType n bi b diteBT :: xs
+
+    /-- Optimize one BRANCH of a `Blaster.dite'`.
+
+        D6 FIX. `forcedBT` is the binder type the `dite'` head demands of this
+        branch, built from the condition `args[1]!` AFTER that condition has been
+        optimized (which has already happened at this point — see the note in
+        `optimizeExplicitArgs`): `p` for the then-branch, `¬ p` for the else-branch.
+
+        Passing it down makes the branch's binder type track the head instead of
+        being optimized independently. Independent optimization is unsound at the
+        KERNEL level even though every rule involved is propositionally valid:
+        `optimizeNot` rewrites `¬(¬e₁ ∧ ¬e₂)` to `e₁ ∨ e₂` and `¬(true = e)` to
+        `false = e`, neither of which is defeq to what `dite' : (p → α) → (¬ p → α)`
+        requires, so `addDecl` on the optimizer's output was rejected with an
+        application type mismatch. Reproductions: the CIP-153 `unionValue`
+        128-bit range guard and `valueContains`'s containment test. -/
     @[always_inline, inline]
-    optimizeDiteArg (e : Expr) (stack : List OptimizeStack) : TranslateEnvT (List OptimizeStack) := do
+    optimizeDiteArg (e : Expr) (forcedBT : Expr)
+                    (stack : List OptimizeStack) : TranslateEnvT (List OptimizeStack) := do
       match e with
-      | Expr.lam n t b bi => return optimizeLambda n t b bi stack (inDite := true)
-      | Expr.mvar _ => optimizeDiteArg (← getMVarValue e) stack
+      | Expr.lam n t b bi => return optimizeLambda n t b bi stack (diteBT := some forcedBT)
+      | Expr.mvar _ => optimizeDiteArg (← getMVarValue e) forcedBT stack
       | _ => return .InitOptimizeExpr e :: stack
 
     @[always_inline, inline]
@@ -238,9 +254,14 @@ partial def optimizeExprAux (stack : List OptimizeStack) : TranslateEnvT Expr :=
      (stack : List OptimizeStack) (nxtStack : List OptimizeStack) : TranslateEnvT (List OptimizeStack) := do
       if isBlasterDiteConst f then
          -- NOTE: optimization on Blaster.dite' cond already performed at this stage
-         -- and idx starts as from 2 (see continuation for DiteChoiceWaitForCond)
-        if idx == 2 || idx == 3
-        then optimizeDiteArg args[idx]! stack
+         -- and idx starts as from 2 (see continuation for DiteChoiceWaitForCond).
+         -- Because args[1]! is ALREADY the optimized condition, it is exactly the
+         -- binder type the then-branch must have, and `¬ args[1]!` exactly the one
+         -- the else-branch must have. Forcing them (D6 fix) keeps the emitted term
+         -- kernel-well-typed.
+        if idx == 2 then optimizeDiteArg args[2]! args[1]! stack
+        else if idx == 3 then
+          optimizeDiteArg args[3]! (← mkAppExpr (← mkPropNotOp) args[1]!) stack
         else return (.InitOptimizeExpr args[idx]! :: stack)
       else if let some argInfo := mInfo then
          -- NOTE: optimization on discriminators already performed at this stage
diff --git a/Blaster/Optimize/OptimizeStack.lean b/Blaster/Optimize/OptimizeStack.lean
index 9e7dfd8..179643f 100644
--- a/Blaster/Optimize/OptimizeStack.lean
+++ b/Blaster/Optimize/OptimizeStack.lean
@@ -47,7 +47,11 @@ inductive OptimizeStack where
  | DiteChoiceWaitForCond (f : Expr) (args : Array Expr) (pInfo : FunEnvInfo)
  | MatchChoiceOptimizeDiscrs (f : Expr) (args : Array Expr) (pInfo : FunEnvInfo)
                              (idx : Nat) (mInfo : MatchInfo)
- | LambdaWaitForType (n : Name) (bi : BinderInfo) (body : Expr) (inDite : Bool)
+ -- `diteBT`: `some t` when this lambda is a `Blaster.dite'` BRANCH, and `t` is
+ -- the binder type FORCED by the (already-optimized) `dite'` condition — `p` for
+ -- the then-branch, `¬ p` for the else-branch. `none` for every other lambda.
+ -- See the D6 fix note in Blaster/Optimize/Basic.lean's `optimizeDiteArg`.
+ | LambdaWaitForType (n : Name) (bi : BinderInfo) (body : Expr) (diteBT : Option Expr)
  | LambdaWaitForBody (x : Expr) (hctx : Option HypsStackContext)
  | MatchRhsLambdaWaitForType (n : Name) (bi : BinderInfo) (body : Expr)
  | MatchRhsLambdaNext (e : Expr)
@@ -256,11 +260,19 @@ def stackContinuity (stack : List OptimizeStack) (optExpr : Expr) (skipCache :=
        -- continuity with optimizing the next discriminator
        return Sum.inl (.MatchChoiceOptimizeDiscrs f (args.set! idx optExpr) pInfo (idx + 1) mInfo :: xs)
 
-  | .LambdaWaitForType n bi body inDite :: xs =>
-       -- optExpr corresponds to optimized lambda type
+  | .LambdaWaitForType n bi body diteBT :: xs =>
+       -- optExpr corresponds to optimized lambda type.
+       -- D6 FIX: for a `Blaster.dite'` branch we DISCARD the independently
+       -- optimized binder type and use the one forced by the `dite'` head's own
+       -- (already optimized) condition. Optimizing the two separately is what
+       -- made the optimizer emit kernel-ill-typed terms: `optimizeNot`'s
+       -- `¬(¬e₁ ∧ ¬e₂) ⇒ e₁ ∨ e₂` and `¬(true = e) ⇒ false = e` rules rewrote the
+       -- else-branch binder to a CLASSICALLY EQUIVALENT BUT NOT DEFEQ
+       -- proposition, while `dite' p t e : (¬ p → α)` still demanded `¬ p`.
+       let optExpr := diteBT.getD optExpr
        withLocalDecl' n bi optExpr fun x => do
          let bodyOpt := .InitOptimizeExpr (← instantiateShared1 body x)
-         if inDite then
+         if diteBT.isSome then
            let ⟨_, ⟨_, _, localRewriteCache, _, _, _, _, _, hypothesisContext, _, _, _, _, _, _⟩⟩ ← get
            let hypsUpdated ← addHypotheses optExpr x
            let hypsCtx ← mkHypStackContext hypsUpdated hypothesisContext localRewriteCache
